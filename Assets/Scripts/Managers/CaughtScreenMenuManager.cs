using TMPro;
using UnityEngine;

public class CaughtScreenMenuManager : MonoBehaviour
{
    public TMP_Text amountOwedText;
    private void Start()
    {
        amountOwedText.text = "$" + "WORKING ON IT...";
    }
}
